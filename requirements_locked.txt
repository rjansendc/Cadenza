# Cadenza — locked environment (Python 3.11.10, conda env "videoeditor")
# Rebuild:
#   conda create -n videoeditor python=3.11.10 -y
#   conda activate videoeditor
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements_locked.txt
#   pip install --no-deps audalign==1.3.1     # optional, not imported by the app
#
# torch MUST come from the cu121 index first, or pip installs the CPU build.
# CUDA 12.1 wheels cover GPUs up to the RTX 40 series; a 50-series card
# needs torch 2.7+ on CUDA 12.8 instead.
#
# opencv-python is deliberately absent: 4.13 requires numpy 2, which breaks
# the numpy 1.26.4 pin, and nothing in the app imports cv2. The optional
# pre-scale in gpu/media/decoder.py stays disabled without it.

PySide6==6.11.0
av==17.0.0
decord==0.6.0
sounddevice==0.5.5
numpy==1.26.4
scipy==1.12.0
matplotlib==3.8.2
pydub==0.25.1
tqdm==4.66.2

# dev / build
pytest==9.0.3
pytest-qt==4.5.0
pyinstaller==6.19.0
